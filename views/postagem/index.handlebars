<div class="card">
    <div class="card-body mt-4">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <small>Data de públicação: {{postagem.data}}</small><br>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>